name: Gabut SSH Tunnel via Zrok

# Trigger manual menggunakan tombol 'Run workflow'
on:
  workflow_dispatch:

jobs:
  oprek-server:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Setup SSH Server and Key
        run: |
          sudo apt-get update
          sudo apt-get install -y openssh-server
          sudo mkdir -p /var/run/sshd
          
          # Setup folder SSH untuk user bawaan GitHub Actions ('runner')
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          
          # Memasukkan SSH Public Key Termux dari GitHub Secrets
          echo "${{ secrets.SSH_PUB_KEY }}" >> ~/.ssh/authorized_keys
          chmod 600 ~/.ssh/authorized_keys
          
          # Menjalankan service SSH
          sudo systemctl start ssh

      - name: 2. Download and Install Zrok
        env:
          # Menggunakan token bawaan Actions agar kebal rate limit API
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd /tmp
          echo "Mencari URL rilis zrok..."
          # Mengambil URL dengan jq agar lebih akurat
          ZROK_URL=$(curl -s -H "Authorization: Bearer $GITHUB_TOKEN" https://api.github.com/repos/openziti/zrok/releases/latest | jq -r '.assets[] | select(.name | endswith("linux_amd64.tar.gz")) | .browser_download_url' | head -n 1)
          
          echo "Mendownload zrok dari: $ZROK_URL"
          curl -L -o zrok.tar.gz "$ZROK_URL"
          
          echo "Mengekstrak file..."
          tar -xzf zrok.tar.gz
          
          echo "Mencari dan memindahkan binary zrok..."
          # Mencari file bernama persis 'zrok' (antisipasi masuk ke sub-folder)
          ZROK_BIN=$(find . -type f -name "zrok" | head -n 1)
          
          sudo mv "$ZROK_BIN" /usr/local/bin/zrok
          sudo chmod +x /usr/local/bin/zrok
          
          # Cek apakah zrok berhasil terinstal
          zrok version

      - name: 3. Enable Zrok and Expose Port 22
        env:
          ZROK_TOKEN: ${{ secrets.ZROK_TOKEN }}
        run: |
          # Autentikasi akun zrok
          zrok enable $ZROK_TOKEN
          
          # Menjalankan zrok di background untuk expose port 22
          # Log diarahkan ke file agar URL bisa kita ekstrak/baca
          zrok share public localhost:22 --backend-mode tcp > zrok_output.log 2>&1 &
          
          # Jeda sebentar agar zrok selesai generate URL di server mereka
          sleep 10
          
          # Menampilkan informasi koneksi ke layar log Actions
          echo "=========================================================="
          echo "🔥 ZROK BERHASIL JALAN! 🔥"
          echo "Cari baris yang mengandung 'access your zrok share' di bawah ini:"
          echo "----------------------------------------------------------"
          cat zrok_output.log
          echo "=========================================================="

      - name: 4. Keep Alive
        run: |
          echo "Runner sedang ditahan agar tidak mati otomatis..."
          echo "PENTING: Jangan lupa klik 'Cancel workflow' di GitHub jika kamu sudah selesai ngoprek!"
          sleep 6h
